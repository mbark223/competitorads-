# Meta Ad Library Monitor - Environment Variables
# Copy this to .env and fill in your values (or add as Vercel Environment Variables)

# ===== DATABASE (REQUIRED FOR PRODUCTION) =====
# Turso database connection (get from https://turso.tech)
TURSO_DATABASE_URL=libsql://your-database-url.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# ===== API KEYS (OPTIONAL) =====
# Required for live Ad Library scraping (get from https://apify.com)
APIFY_TOKEN=your_apify_token_here

# Optional: Custom Apify actor ID for Meta Ad Library scraping
# META_AD_SCRAPER_ID=your_custom_actor_id

# Required for AI analysis (get from https://ai.google.dev)
GEMINI_API_KEY=your_gemini_api_key_here

# ===== SERVER CONFIG =====
# Optional - defaults to 3000 (5000 is used by macOS AirPlay)
PORT=3000

# Note: App runs in demo mode with mock data if API keys are not provided
# Note: Database will use local file if Turso credentials are not provided (local dev only)
